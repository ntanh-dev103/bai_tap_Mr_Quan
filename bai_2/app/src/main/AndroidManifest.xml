<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.AndroidUIBaiTap"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".AddStudentActivity" />
        <activity android:name=".StudentListActivity" />
        <activity android:name=".ClassHomeActivity" />
        <activity android:name=".ProfileTabActivity" />
        <activity android:name=".SubjectListActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PersonalInfoActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
